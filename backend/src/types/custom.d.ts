/**
 * @page
 * JSON型の詳細定義
 * フロント側に自動生成する型定義で利用される
 *
 * 第一階層はテーブル名
 * 第二階層はカラム名
 * 第三階層は型
 */

export interface JSONTypeMap {
  part: {
    position: {
      x: number;
      y: number;
    };
  };
}
